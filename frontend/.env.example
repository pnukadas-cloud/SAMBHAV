# SAMBHAV Frontend Environment Configuration Template
# Production/Staging API base URL (leave empty if using Firebase Hosting rewrites to Cloud Run on same domain)
# In local development, defaults to http://127.0.0.1:8000
VITE_API_BASE_URL=
