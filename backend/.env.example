# ==========================================
# SAMBHAV ENVIRONMENT CONFIGURATION
# ==========================================

# Database: Default SQLite for zero-setup local dev. Use postgresql://... in production.
DATABASE_URL=sqlite:///sambhav.db

# JWT & Authentication Secret
JWT_SECRET_KEY=sambhav_quantum_production_secret_key_2026_dev_env

# Google Gemini API Key for Quantum AI Tutor
GEMINI_API_KEY=

# SMTP Email Configuration for 2-Factor OTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS=True

# Server Configuration
PORT=8000
HOST=127.0.0.1
ENVIRONMENT=development
